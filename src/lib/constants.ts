// ============================================================
// constants.ts — 앱 전체에서 공유하는 상수 모음
// 모델명이나 제한값을 바꾸고 싶다면 이 파일만 수정하세요.
// ============================================================

// ─── [2단계] Gemini 모델 선택 ────────────────────────────
// 무료 한도 안에서 쓸 수 있는 모델 (billing 없이 테스트 가능):
//
//   "gemini-1.5-flash"       → ✅ 권장. 안정적이고 무료 한도 넉넉함
//   "gemini-1.5-flash-8b"    → 가장 가벼움 (응답 품질 약간 낮음)
//   "gemini-2.0-flash"       → 최신, 무료 한도 있음 (한도 더 빠듯함)
//
// ⚠️ "gemini-2.0-flash-lite"는 아직 API에서 불안정할 수 있어 gemini-1.5-flash 권장
export const GEMINI_MODEL = "gemini-1.5-flash";

// ─── [3단계] 비용 절감 설정 ──────────────────────────────
// 서버로 보낼 최근 대화 수.
// 숫자가 클수록 AI가 더 많은 맥락을 기억하지만 토큰 소비가 늘어납니다.
// todo 체크 용도라면 6~8이 적당합니다.
export const MAX_HISTORY = 8;

// AI 응답 최대 토큰 수.
// todo 목록 요약은 짧아도 충분하므로 256 정도로 충분합니다.
// 더 긴 설명이 필요하면 512 이상으로 올리세요.
export const MAX_OUTPUT_TOKENS = 256;

// 응답의 창의성 조절 (0 = 일관되고 정확, 1 = 창의적)
// todo 관련 질문은 정확한 답변이 중요하므로 낮게 설정합니다.
export const TEMPERATURE = 0.3;

// ─── [1단계] 시스템 프롬프트 ─────────────────────────────
// AI의 성격·역할을 정의합니다. 짧게 유지할수록 비용이 줄어듭니다.
// 말투나 역할을 바꾸고 싶다면 이 부분만 수정하세요.
//
// 할 일 목록은 아래 형식으로 자동 추가됩니다:
//   [현재 할 일 목록]
//   - [ ] ☀️ 아침 할 일 제목   ← 미완료
//   - [x] 🌙 저녁 할 일 제목   ← 완료
export const SYSTEM_PROMPT = `당신은 할 일 관리를 함께 도와주는 AI 어시스턴트입니다.

메시지에 [현재 할 일 목록]이 포함되면:
- [x]는 완료된 항목, [ ]는 미완료 항목입니다
- ☀️는 아침(Morning), 🌙는 저녁(Evening) 구분입니다
- 사용자가 묻지 않아도 남은 할 일을 간략히 정리해 주세요

규칙:
- 항상 한국어로 답변
- 2~4문장 이내로 간결하게
- 불필요한 인사말·서론 없이 바로 답변`;
